// Root build file for lynx.open.sdk. Pins match the upstream Alpha2OpenSdk repo
// exactly (see its AGENTS.md / build.gradle) so the AIDL + Java sources here build
// unmodified against the same Android Gradle Plugin version.
plugins {
    id 'com.android.library' version '4.2.2' apply false
}
