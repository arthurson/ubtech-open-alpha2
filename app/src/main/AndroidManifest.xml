<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.open.lynx">

    <!--
      This app is a thin test panel for lynx-open-sdk
      (com.ubtechinc.alpha2serverlib.aidlinterface, confirmed against
      com.ubtechinc.alpha2services.apk). It has one Activity with a button per AIDL
      call and a scrolling log - no HTTP/WebSocket server, camera, or audio recording.
    -->

    <application
        android:allowBackup="true"
        android:label="Open Lynx"
        android:icon="@android:drawable/ic_menu_manage"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
